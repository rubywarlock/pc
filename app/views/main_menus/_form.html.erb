<%= form_for(@main_menu) do |f| %>
  <% if @main_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@main_menu.errors.count, "error") %> prohibited this main_menu from being saved:</h2>

      <ul>
      <% @main_menu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mainmenuname %><br />
    <%= f.text_field :mainmenuname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
